export const environment = {
  production: true,
  /** Base URL answered by the in-browser mock backend (the default data source). */
  apiUrl: '/api',
  /** Base URL of the live ASP.NET Core API, used when the user switches to it at runtime. */
  liveApiUrl: 'https://api.antrias.site/api',
};
